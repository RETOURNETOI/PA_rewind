RewriteEngine On
RewriteBase /PA_rewind/public/

# Si le fichier ou dossier existe, ne rien faire
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Sinon, redirige tout vers index.php
RewriteRule ^ index.php [L]
